#include <stdio.h>

int main()
{
    int qtd, carro , i, v, c, k, x;
    scanf("%d", &qtd);
    int vetorC[qtd];
    for (i = 0; i < qtd + 1; i++)
    {
        scanf("%c %d %d", &c , &k, &v);
        if (c == 'i')
        {
            for (x = 0; i < count; i++)
            {
                /* code */
            }
            
        }
        
    }
    for (i = 1; i < x + 1; i++)
    {

    }
    for (i = 1; i < x + 1; i++)
    {

    }
    return 0;
}