#include <stdio.h>

int main(void){
    int x;
    scanf("%d", &x);
    char vetorC[x], letra;
    for (int i = 0; i < x; i++)
    {
        scanf("%c ", vetorC[x]);
    }
    printf("\n");
    
    
}